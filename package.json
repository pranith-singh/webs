{
  "name": "azure-node-sql-app",
  "version": "1.0.0",
  "description": "Sample Node.js app with Azure SQL connection",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "test": "echo \"No tests specified\" && exit 0"
  },
  "dependencies": {
    "express": "^4.19.2",
    "mssql": "^10.0.1"
  }
}
